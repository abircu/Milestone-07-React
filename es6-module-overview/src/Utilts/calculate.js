const add = (first, second) => {
  return first + second;
};
const multiply = (first, sec) => {
  return first * sec;
};
const diviteTheFirstNumberByTheSecondNumber = (first, second) => {
  return first / second;
};
export { add, multiply, diviteTheFirstNumberByTheSecondNumber };
